# Docker image to use for building
image: jekyll/jekyll:3.8
# Build command
command: gem install bundler --source http://rubygems.org, bundle update -bundler, jekyll build
# Public directory
public: _site