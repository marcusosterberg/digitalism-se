# Docker image to use for building
image: williamjackson/pelican
# Build command
command: mkdir my-site && cd my-site && docker container run -it --rm --entrypoint pelican-quickstart -v ${PWD}:/my-site williamjackson/pelican
# Public directory
public: _site