# Docker image to use for building
image: williamjackson/pelican
# Build command
command: pelican content
# Public directory
public: output
domains:
  # Primary domain
  - digitalism.se
  # Domains that redirect to the primary
  - www.digitalism.se